_Auto-generated route/surface map. Source: `project-metadata/architecture/routes.yaml`._

_Generated: {{ generated_at }}_

| Surface | Kind | Owning feature | Source file | Triggered by |
|---|---|---|---|---|
{% for r in routes -%}
| **{{ r.name }}** | `{{ r.kind.value }}` | {{ r.owning_feature or '—' }} | `{{ r.source_file }}` | {% if r.triggered_by %}{{ r.triggered_by|join(', ') }}{% else %}—{% endif %} |
{% endfor %}

{% if not routes -%}
_No routes registered. Run `context-engine generate --routes` to seed surfaces from `App.tsx` and the overlay/modal entry points._
{%- endif %}
