_Auto-generated from `project-metadata/features/*.yaml`._

_Scan: {{ generated_at }}_

## Shipped

{% if shipped_features -%}
| Feature | Confidence | Files | Tests | Description |
|---|---|---|---|---|
{% for f in shipped_features -%}
| **{{ f.name }}** | `{{ f.confidence.value }}` | {{ f.source_files|length }} | {{ f.tests|length }} | {{ (f.description or '—')|replace('|', '\\|')|truncate(160, true, '…') }} |
{% endfor -%}
{% else -%}
_None._
{%- endif %}

## In progress

{% if in_progress_features -%}
| Feature | Description |
|---|---|
{% for f in in_progress_features -%}
| **{{ f.name }}** | {{ (f.description or '—')|replace('|', '\\|')|truncate(160, true, '…') }} |
{% endfor -%}
{% else -%}
_None._
{%- endif %}

## Planned

{% if planned_features -%}
| Feature | Description |
|---|---|
{% for f in planned_features -%}
| **{{ f.name }}** | {{ (f.description or '—')|replace('|', '\\|')|truncate(160, true, '…') }} |
{% endfor -%}
{% else -%}
_None._
{%- endif %}

{% if deprecated_features -%}
## Deprecated

| Feature | Notes |
|---|---|
{% for f in deprecated_features -%}
| **{{ f.name }}** | {{ (f.description or '—')|replace('|', '\\|')|truncate(160, true, '…') }} |
{% endfor -%}
{% endif %}
