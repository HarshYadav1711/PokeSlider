_Auto-generated mirror of `PROJECT_CONTEXT.md` onboarding capsule for Cursor rules. Edit `project-metadata/` and regenerate; do not hand-edit between markers._

| Field | Current value |
|---|---|
| Files scanned | {{ stats.file_count }} |
| Shipped features | {{ stats.shipped_feature_count }} / {{ stats.feature_count }} |
| Systems | {{ stats.system_count }} |
| Surfaces (routes) | {{ stats.route_count }} |
| Tests | {{ stats.test_count }} |
| Generated | {{ generated_at }} |

## Hard rules for AI sessions

1. **Implementation truth is the source of truth.** Anything not represented in `project-metadata/` is unverified.
2. Before claiming a feature is shipped, confirm it exists in `project-metadata/features/<id>.yaml` AND that the listed source files exist on disk.
3. Do not introduce a second client-state store or a second AST parsing pipeline.
4. Do not modify generated documentation by hand; edit YAML and run `context-engine generate`.

## Verified layers

{% for layer, files in layers.items() -%}
- **{{ layer }}** — {{ files|length }} files
{% endfor %}

## Dangerous modification zones

{% for path, score in hotspots[:5] -%}
- `{{ path }}` (coupling {{ score }})
{% endfor %}
