_Auto-generated. Edit YAML under `project-metadata/features/` and re-run `context-engine generate`._

_Scan: {{ generated_at }}{% if git_commit %} · `{{ git_commit[:10] }}`{% endif %}{% if git_branch %} · `{{ git_branch }}`{% endif %}{% if git_dirty %} · dirty{% endif %}_

## Identity

PokeSlider — React 19 + Vite 8 + TypeScript + Tailwind 4 SPA over the public PokéAPI. Hero 3D Poké Ball carousel, overlay panels for ball/Pokémon detail, My Dex discovery, transparent Compare, local rule-based Team Builder, and Battle Simulator. No backend in repo.

## State

| Metric | Value |
|---|---|
| Files scanned | {{ stats.file_count }} |
| TypeScript / TSX | {{ stats.ts_count }} |
| Tests | {{ stats.test_count }} |
| Shipped features | {{ stats.shipped_count }} / {{ stats.feature_count }} |

## Shipped features

{% for f in shipped_features -%}
- **{{ f.name }}** — _{{ f.confidence.value }}_ — {{ f.description or '—' }}
{% endfor -%}
{% if not shipped_features %}_None recorded._{% endif %}

## In progress

{% if in_progress_features -%}
{% for f in in_progress_features -%}
- **{{ f.name }}** — {{ f.description or '—' }}
{% endfor -%}
{% else -%}
_Nothing in-flight._
{%- endif %}

## Planned

{% if planned_features -%}
{% for f in planned_features -%}
- **{{ f.name }}** — {{ f.description or '—' }}
{% endfor -%}
{% else -%}
_None._
{%- endif %}

## Shared systems referenced

{% if shared_systems -%}
{{ shared_systems|join(', ') }}
{%- else -%}
_None referenced._
{%- endif %}

_Source-of-truth order: implementation → `project-metadata/features/` → generated docs → human prose in MANUAL-NOTES._
